const divide = (num1, num2) => {
    return num1 / num2;
}
divide (2000,100);

const square = (number) => {
    return number * number;
}
square(4)

const add = (parameter1, parameter2) => parameter1 + parameter2;
add(20,50);